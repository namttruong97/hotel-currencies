```
    project
    │   tsconfig.json
    │
    └───pages: router for NextJs
    └───src
    │   │
    │   └─── assets: icons, images, ...
    │   │
    │   └─── components: small component to re-used
    │   │
    │   └─── container: big component include a few components
    │   │
    │   └─── constants: declare constant for project
    │   │
    │   └─── hooks: custom hooks
    │   │
    │   └─── models: structure of entities
    │   │
    │   └─── pages: react-admin router
    │   │
    │   └─── providers: config provider for app
    │   │
    │   └─── services: connect to BE by Axios
    │   │
    │   └─── store: atom store - global store
    │   │
    │   └─── styles: custom theme
    │   │
    │   └─── utils: errors code, helper function, timezone
    │   │
    │   └─── ...
```
